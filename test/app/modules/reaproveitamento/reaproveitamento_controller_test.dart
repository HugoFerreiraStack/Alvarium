import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:alvarium/app/modules/reaproveitamento/reaproveitamento_controller.dart';
import 'package:alvarium/app/modules/reaproveitamento/reaproveitamento_module.dart';

void main() {
  initModule(ReaproveitamentoModule());
  // ReaproveitamentoController reaproveitamento;
  //
  setUp(() {
    //     reaproveitamento = ReaproveitamentoModule.to.get<ReaproveitamentoController>();
  });

  group('ReaproveitamentoController Test', () {
    //   test("First Test", () {
    //     expect(reaproveitamento, isInstanceOf<ReaproveitamentoController>());
    //   });

    //   test("Set Value", () {
    //     expect(reaproveitamento.value, equals(0));
    //     reaproveitamento.increment();
    //     expect(reaproveitamento.value, equals(1));
    //   });
  });
}
