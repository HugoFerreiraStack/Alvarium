import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:alvarium/app/modules/reaproveitamento/reaproveitamento_page.dart';

void main() {
  testWidgets('ReaproveitamentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReaproveitamentoPage(title: 'Reaproveitamento')));
    //  final titleFinder = find.text('Reaproveitamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
