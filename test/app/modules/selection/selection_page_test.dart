import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:alvarium/app/modules/selection/selection_page.dart';

void main() {
  testWidgets('SelectionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SelectionPage(title: 'Selection')));
    //  final titleFinder = find.text('Selection');
    //  expect(titleFinder, findsOneWidget);
  });
}
