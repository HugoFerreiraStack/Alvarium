import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:alvarium/app/modules/selection/selection_controller.dart';
import 'package:alvarium/app/modules/selection/selection_module.dart';

void main() {
  initModule(SelectionModule());
  // SelectionController selection;
  //
  setUp(() {
    //     selection = SelectionModule.to.get<SelectionController>();
  });

  group('SelectionController Test', () {
    //   test("First Test", () {
    //     expect(selection, isInstanceOf<SelectionController>());
    //   });

    //   test("Set Value", () {
    //     expect(selection.value, equals(0));
    //     selection.increment();
    //     expect(selection.value, equals(1));
    //   });
  });
}
